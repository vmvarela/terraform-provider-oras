# Project Principles

This is an experimental Terraform pluggable StateStore implementation using OCI registries.

Priority: correctness → explicit guarantees → upstream compatibility → reproducible evidence/tests → small abstractions → operational usefulness.

Experimental does not mean careless.

Prefer the smallest correct change. Avoid speculative abstractions. Separate facts, observations, assumptions and hypotheses. Never generalize registry-specific observations without evidence.
